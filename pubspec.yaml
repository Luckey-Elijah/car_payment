name: car_payment
description: "How Much Car Can You Afford?"
publish_to: "none"
version: 1.1.0+1
flutter: { uses-material-design: false }
environment: { sdk: "^3.8.0", flutter: "3.32.0" }

dependencies:
  context_plus: ^6.0.6
  flutter: { sdk: flutter }
  shadcn_ui: ^0.50.2
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^6.0.0
